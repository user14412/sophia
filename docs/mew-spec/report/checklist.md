# Agent 课程大作业报告 Checklist

> 本清单用于约束后续正文写作、图表制作、LaTeX 排版和最终 PDF 交付。每一项都应通过查看文件、打开 PDF 或检查正文内容来验证。

## 作业硬性要求

- [ ] 首页显式标注选题编号 `E（工程实践与设计）`（验证：打开最终 PDF 首页可见）。
- [ ] 首页署名位置包含姓名 `昝研`（验证：打开最终 PDF 首页可见）。
- [ ] 首页署名位置包含学号 `522025330141`（验证：打开最终 PDF 首页可见）。
- [ ] 最终 PDF 文件名为 `522025330141昝研.PDF`（验证：检查 `docs/report/output/` 中输出文件名）。
- [ ] 报告采用软件学报风格结构，包含标题、作者信息、摘要、关键词、正文分节、图表和参考文献（验证：查看 LaTeX/PDF 结构）。
- [ ] 正文开头 100 字内说明本文与 Agent 新技术的关系，并标注选题 E（验证：查看正文第一段）。
- [ ] 报告包含 AI 使用说明（验证：存在独立小节或明确段落）。

## 内容结构

- [ ] 标题为《面向教育播客生成的多阶段 Agent 工作流设计与原型实现》或等价表述（验证：查看标题页）。
- [ ] 摘要说明背景、问题、方法和原型意义（验证：摘要不少于 4 个信息点）。
- [ ] 关键词包含“大模型智能体”“多智能体协作”“工作流编排”等 Agent 相关词（验证：查看关键词）。
- [ ] 正文按已批准大纲组织，包含引言、需求分析、系统架构、多阶段工作流、案例分析、工程化讨论、AI 使用说明和总结（验证：查看目录或分节标题）。
- [ ] 引言聚焦知识播客生产痛点和长文本生成问题，不写近期调试细节（验证：搜索正文中是否出现端口、上传失败、代理等无关词）。
- [ ] 需求分析将“知识锚点、听众嘴替、幕后统筹”映射为 Agent 职责（验证：查看第 2 节）。
- [ ] 系统架构说明知识层、内容层、表现层三层结构（验证：查看第 3 节）。
- [ ] 多阶段工作流重点说明 Topic、Director、Agent Speechers 的职责和衔接（验证：查看第 4 节）。
- [ ] 上下文机制说明 System Prompt、Current Task、Memory、Knowledge Base 四类上下文（验证：查看第 4 节或表 2）。
- [ ] 案例分析使用 `ch01-demo` 的 topic、director、script 等现有产物，不额外编造实验数据（验证：查看第 5 节）。
- [ ] 工程化讨论包含可解释、可调试、可扩展、人机协同和局限性（验证：查看第 6 节）。
- [ ] 总结回扣选题 E 和 Agent 工程实践价值（验证：查看最后一节）。

## 图表与素材

- [ ] 至少包含 2 个图表或表格（验证：PDF 中能看到至少图 1 和表 1）。
- [ ] 图 1 展示系统总体工作流：参考资料、Topic、Director、Agent Speechers、Voice/Image、Editor、播客视频或摘要（验证：查看图 1）。
- [ ] 表 1 展示阶段职责与产物，包含阶段、输入、输出、作用（验证：查看表 1）。
- [ ] 若篇幅允许，表 2 展示上下文组织机制（验证：查看表 2；若未保留，则确认已有图 1 和表 1）。
- [ ] 图表文字不包含端口、manifest 报错、上传失败等调试词（验证：查看图表标题和内容）。
- [ ] 案例引用来自已有中间产物，至少包含两个主题名称和一段双人脚本示例（验证：查看第 5 节）。

## 真实性与表述边界

- [ ] 不编造作者、学号、学院等个人信息（验证：与 `pending-info.md` 一致）。
- [ ] 不编造大规模实验、用户研究、准确率、耗时提升等硬数字（验证：搜索百分号、秒数、样本量等量化表述，确认有来源或已删除）。
- [ ] 不写成项目 README、操作手册或调试日志（验证：正文以论文分节和论证为主）。
- [ ] 不详细暴露近期工程整理中的端口占用、前端代理、环境变量、上传失败、manifest 报错等细节（验证：全文搜索相关词）。
- [ ] 若提到系统能力，使用“原型支持”“设计为”“可扩展为”“案例产物表明”等谨慎表述（验证：查看架构和案例段落）。
- [ ] 外部参考文献控制在 4 条左右，且均可核验（验证：检查参考文献条目）。
- [ ] 正文采用顺序编码制 `[1]`、`[2]`、`[3]`、`[4]`，引用编号与文末条目一致（验证：全文搜索引用编号）。
- [ ] 参考文献只服务核心论点：ReAct、RAG、多智能体会话、LangGraph/工作流编排（验证：检查正文引用位置）。
- [ ] 不把 GPT-SoVITS、FFmpeg、Vite、FastAPI 等未展开讨论的工程依赖堆入参考文献（验证：检查参考文献列表）。

## 排版与 PDF

- [ ] Markdown 正文存在于 `docs/report/agent-podcast-report.md`（验证：文件存在且内容完整）。
- [ ] LaTeX 正文存在于 `docs/report/latex/main.tex`（验证：文件存在且结构完整）。
- [ ] 图表文件存在于 `docs/report/figures/` 或已在 LaTeX 中用表格替代（验证：目录和正文引用一致）。
- [ ] 最终 PDF 能正常打开（验证：打开 `docs/report/output/522025330141昝研.PDF`）。
- [ ] PDF 首页无明显乱码、缺字、重叠或空白图（验证：打开 PDF 视觉检查）。
- [ ] PDF 中摘要、关键词、正文、AI 使用说明和参考文献均可读（验证：浏览 PDF 全文）。

## 最终提交前检查

- [ ] 提交链接以新链接为准：`https://box.nju.edu.cn/d/43708e2745ea4df8b438/`（验证：最终提醒中使用新链接）。
- [ ] 输出文件名大小写符合要求：`522025330141昝研.PDF`（验证：文件系统检查）。
- [ ] 最终回复中明确说明 PDF 路径、是否成功生成、是否还有需要用户手动提交的动作（验证：最终交付说明）。
